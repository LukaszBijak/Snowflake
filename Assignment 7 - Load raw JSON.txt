CREATE STAGE "MANAGE_DB"."EXTERNAL_STAGES".JSONSTAGE
    URL = 's3://snowflake-assignments-mc/unstructureddata/';
LIST @"MANAGE_DB"."EXTERNAL_STAGES".JSONSTAGE;

CREATE file format JSONFORMAT
    TYPE = JSON;

CREATE TABLE "MANAGE_DB"."PUBLIC".JSON_RAW(
    Raw Variant);
    
COPY INTO "MANAGE_DB"."PUBLIC".JSON_RAW
    FROM @"MANAGE_DB"."EXTERNAL_STAGES".JSONSTAGE
    FILE_FORMAT = JSONFORMAT
    FILES = ('Jobskills.json');